%li
  =link_to 'Artists', '#', :class => "header with_sub #{active?("artists")}"
  %ul#artists.sub_menu{:class => active?("artists") }
    -artists.each do |artist|
      %li=link_to artist.get_name, artist_path(artist), :class => 'sub_link'